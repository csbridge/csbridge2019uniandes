import graphics.*;
import acm.graphics.*;
import acm.program.*;
import java.awt.Color;

public class MinLoco extends EsConsole {
	public void run() {
	    imprimir("El minimo de 4 y 5 es: " + min(4, 5));
	    imprimir("El minimo de -4 y 4 es: " + min(-4, 4));
	    imprimir("El minimo de 1 y 2 es: " + min(1, 2));
	    imprimir("El minimo de 0 y 0 es: " + min(0, 0));
	}

	/** 
	 * Methodo: Min
	 * -----------
	 * El metodo min (minimo) toma dos entradas (i y j) 
	 * y devuelve la menor de las dos.
	 */
	private int min(int i, int j){
		// tu codigo va aca
		return 0;
	}
}